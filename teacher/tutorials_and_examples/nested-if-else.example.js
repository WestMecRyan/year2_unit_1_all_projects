// let's call 'metric' or 'imperial' factorSystem
// let's call 'kg', 'cm', 'in', 'lbs' or 'repetitions' factorUnit

let factorUnit = "repetitions";
// choose two options

if (factorSystem = "metric") {
    if {} else { }
} else if (factorSystem = "imperial") {
    if {} else { }
}