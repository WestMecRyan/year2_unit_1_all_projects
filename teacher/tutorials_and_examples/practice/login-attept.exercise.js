/*
Create a function called loginAttempt that takes a username and
password as parameters. It should return a
promise that resolves if the username is "admin"
and the password is "1234", and rejects otherwise.
Use setTimeout to simulate a 1-second delay
before resolving or rejecting.
*/